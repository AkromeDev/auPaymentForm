import { MilesConverterPipe } from './miles-converter.pipe';

describe('MilesConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MilesConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
